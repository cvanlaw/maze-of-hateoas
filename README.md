# Maze of HATEOAS

A an API-based maze using the principles of HATEOAS (Hypermedia as the engine of application state).

## Endpoints

### /generate

Generates a new maze and returns its location

### /mazes

List the available mazes and their locations